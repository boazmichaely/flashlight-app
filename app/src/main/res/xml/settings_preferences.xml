<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Version row (non-selectable) -->
    <Preference
        android:key="app_version"
        android:title="@string/settings_version_title"
        android:summary="@string/settings_version_summary"
        android:selectable="false" />

    <!-- Dummy row for spacing confirmation -->
    <Preference
        android:key="dummy_1"
        android:title="Dummy Setting 1"
        android:summary="For layout verification"
        android:selectable="false" />

    <!-- Another dummy row for spacing confirmation -->
    <Preference
        android:key="dummy_2"
        android:title="Dummy Setting 2"
        android:summary="For divider testing"
        android:selectable="false" />

</PreferenceScreen>
